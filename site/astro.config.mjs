// @ts-check
import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';

// GitHub Pages serves a PROJECT site from a subpath
// (https://kuhlman-labs.github.io/fishhawk/), so both `site` and `base` are
// required. Omitting `base` emits root-absolute asset and link URLs that
// resolve under `astro dev` and 404 in production — the exact defect
// site/README.md's verification step greps the built index.html for.
export default defineConfig({
  site: 'https://kuhlman-labs.github.io',
  base: '/fishhawk',
  integrations: [
    starlight({
      title: 'Fishhawk',
      description:
        'The governed, auditable workflow for agent-driven software development.',
      social: [
        {
          icon: 'github',
          label: 'GitHub',
          href: 'https://github.com/kuhlman-labs/fishhawk',
        },
      ],
      // The sidebar is declared EXPLICITLY rather than autogenerated: it is the
      // machine-readable statement of the information architecture, so a
      // missing or misfiled page is a build error instead of a silent
      // reordering. Group order is the IA order from #2261.
      //
      // There are deliberately NO per-spec-major pages. All three live majors
      // are carried as CONTENT — a support table on reference/index.md plus one
      // reference/workflow-spec.md — mirroring docs/spec/workflow-v2.md, which
      // is the complete standalone reference for the live major while v0 and v1
      // are frozen. See reference/versioning.md for the reasoning.
      sidebar: [
        {
          label: 'Start here',
          items: [
            { slug: 'start/what-fishhawk-is' },
            { slug: 'start/what-fishhawk-is-not' },
            { slug: 'start/first-run' },
          ],
        },
        {
          label: 'Concepts',
          items: [
            { slug: 'concepts' },
            { slug: 'concepts/workflow' },
            { slug: 'concepts/stage' },
            { slug: 'concepts/gate' },
            { slug: 'concepts/constraint' },
            { slug: 'concepts/plan' },
            { slug: 'concepts/audit-log' },
          ],
        },
        {
          label: 'Operating',
          items: [
            { slug: 'operating/driving-a-run' },
            { slug: 'operating/approvals' },
            { slug: 'operating/when-a-run-fails' },
          ],
        },
        {
          label: 'Reference',
          items: [
            { slug: 'reference' },
            { slug: 'reference/workflow-spec' },
            { slug: 'reference/plan-schema' },
            { slug: 'reference/cli' },
            { slug: 'reference/api' },
            { slug: 'reference/versioning' },
          ],
        },
      ],
    }),
  ],
});
